# Copy to .env and replace the placeholder locally. Never commit a real key.
TYPESAFE_API_KEY=your_typesafe_api_key_here

# Optional model override; all demos default to jev-latest.
TYPESAFE_MODEL=jev-latest
