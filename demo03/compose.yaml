name: typesafe-demo03-jev-drives
services:
  app:
    build: .
    ports:
      - "127.0.0.1:${DEMO03_PORT:-3003}:3000"
    env_file:
      - ../.env
    environment:
      PORT: "3000"
      NODE_ENV: production
    init: true
    read_only: true
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
